#include <iostream>
#include <math.h>
#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using ll = long long;
using namespace std;
#include <iostream>
#include <numeric>
#include <cstdint>
#include <limits>


int main() {
	int N, D;
	cin >> N >> D;

	set<int> set_a;
	rep(i, N) {
		int a;
		cin >> a;
		set_a.insert(a);
	}

	

	return 0;
}
